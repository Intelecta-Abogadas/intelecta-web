# Base path for GitHub Pages deployment (project repo served under /intelecta-web).
# Set to /intelecta-web for production builds deployed to GitHub Pages.
# Local development can leave this empty so the app is served from the root (/).
NEXT_PUBLIC_BASE_PATH=/intelecta-web
